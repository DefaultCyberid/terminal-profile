# some more ls aliases
alias ls='logo-ls'
alias ll='ls -alh'
alias la='ls -A'

# oh-my-posh theme
eval "$(oh-my-posh --init --shell bash --config ~/.poshthemes/default.omp.json)"

# ble.sh
source ~/.local/share/blesh/ble.sh

# aliases
